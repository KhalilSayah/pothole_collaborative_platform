# Supabase — Project Settings > API.
# Use the ANON public key, never the service_role key: this is a client app and
# anything here ships to every user's browser. RLS is what protects the data.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
